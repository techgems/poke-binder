@model LandingHeader

<header class="absolute inset-x-0 top-0 z-50">
    <nav aria-label="Primary" class="mx-auto flex max-w-7xl items-center justify-between px-6 py-5">
        @Model.RenderSlot(LandingHeader.LogoSlot)
        @Model.RenderSlot(LandingHeader.OptionsSlot)
    </nav>
</header>
